const { ethers } = require('ethers');
console.log(ethers.id("AgentActive()").substring(0, 10));
console.log(ethers.id("AlreadyActive()").substring(0, 10));
console.log(ethers.id("IsActive()").substring(0, 10));
